# dotfiles
A setup for using `GNU stow` and `git` to handle my dotfiles.

For full setup, run
```sh
stow -t ~/ \
    applications \
    clang \
    dunst \
    electron \
    hypr \
    kitty \
    nvim \
    paru \
    rofi \
    scripts \
    tmux \
    user-dirs \
    waybar \
    zsh
```
